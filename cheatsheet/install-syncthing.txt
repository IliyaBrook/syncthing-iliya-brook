from https://computingforgeeks.com/how-to-install-and-use-syncthing-on-ubuntu/


10.10.10.168:8384


systemctl status syncthing@root.service

1.
sudo apt install curl apt-transport-https

2.
curl -s https://syncthing.net/release-key.txt | sudo apt-key add -

3.
echo "deb https://apt.syncthing.net/ syncthing release" | sudo tee /etc/apt/sources.list.d/syncthing.list

4.
sudo apt update
5.
sudo apt install syncthing

6.
syncthing --version

7.
sudo nano /etc/systemd/system/syncthing@.service

8.
copy content to service file:

////////////////////////


[Unit]
Description=Syncthing - Open Source Continuous File Synchronization for %I
Documentation=man:syncthing(1)
After=network.target

[Service]
User=%i
ExecStart=/usr/bin/syncthing -no-browser -gui-address="0.0.0.0:8384" -no-restart -logflags=0
Restart=on-failure
SuccessExitStatus=3 4
RestartForceExitStatus=3 4

[Install]
WantedBy=multi-user.target

////////////////////////

9.
sudo systemctl daemon-reload

10.
sudo systemctl start syncthing@$USER
11
sudo systemctl enable syncthing@$USER

12.
systemctl status syncthing@username.service

13.
sudo ufw allow 22000/tcp


14.
systemctl status syncthing@$USER.service

check on windows if gui work on 10.10.10.168:8384

~/.config/syncthing/config.xml

kill process:
sudo pkill -f "process name"

// check folder space
